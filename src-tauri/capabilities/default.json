{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The reviewed main and code-navigation webviews. Native review work is exposed through explicit commands; filesystem and shell plugins are deliberately absent.",
  "windows": ["main", "symbol-navigation-*"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize"
  ]
}
